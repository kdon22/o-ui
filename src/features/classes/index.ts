/**
 * Classes Feature - Main Exports
 * 
 * Centralized exports for the global classes system including:
 * - Schemas and types
 * - UI components (future)
 */

// Schema
export { CLASS_SCHEMA } from './classes.schema';

// Types (to be created later if needed)
// export type { ClassEntity, CreateClassInput, UpdateClassInput } from './types'; 