'use client'

import EditorLayout from '@/components/layout/editor/editor-layout'

export default function ClassesPage() {
  return (
    <EditorLayout 
      classId={null}
      initialClass={null}
    />
  )
} 