# API Configuration
API_BASE_URL="http://localhost:3000/api"
API_KEY="development-api-key"

# Database Connection (overrides .env)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/o"

# Next Auth Configuration
NEXTAUTH_SECRET="your-nextauth-secret-key-for-jwt-encryption"
NEXTAUTH_URL="http://localhost:3000"

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# NextAuth Configuration
NEXTAUTH_URL_INTERNAL="http://localhost:3000"
NEXTAUTH_DEBUG=false
DEBUG_ENABLED=false

# Resend Email Configuration
RESEND_API_KEY=re_dKXoEq4e_FBBAGYJQkhJkBnbjPfirndNR
EMAIL_FROM="onboarding@resend.dev"

# These are kept here for completeness but NextAuth directly uses the Resend implementation
EMAIL_SERVER_HOST="smtp.resend.com"
EMAIL_SERVER_PORT=465
EMAIL_SERVER_USER="resend"
EMAIL_SERVER_PASSWORD=${RESEND_API_KEY}

# Email configuration - Development Testing Mode
# Nodemailer will create an Ethereal test account automatically
# Check console logs for preview URL when sending emails
EMAIL_FROM=k.donner@me.com

# Email Configuration (iCloud SMTP)
SMTP_HOST=smtp.mail.me.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=k.donner@me.com
SMTP_PASS=ofbr-slwb-fkrw-gqnh
SMTP_FROM=k.donner@me.com

# PyRight server configuration
PYRIGHT_SERVER_URL=http://localhost:3002 